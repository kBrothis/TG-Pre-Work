console.log(Math.floor(Math.random()*100));

console.log(Math.ceil(43.8));

console.log(Number.isInteger(2017))

//built in objects are available for Javascript to make more complex operations easier to code. There objects also have methods that are already built into Javascript for full functionality.
