console.log('Hello' + ' ' + 'World');

//strings can be combined by using the + character to concatenate the strings together. Concatenation does not include spaces so the spaces will need to be added in the strings.
