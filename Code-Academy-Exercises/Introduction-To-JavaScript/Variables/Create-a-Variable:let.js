let changeMe = true;
console.log(changeMe);
changeMe = false;
console.log(changeMe);

//let is another keyword that signals that the variable can be reassigned a different value;
//let can be declared without assigning it a value which automatically assigns it a value of undefined
