const entree = 'Enchiladas';
console.log(entree);
entree = 'Tacos';

//The const keyword is another keyword used for variables, short for constant.
//const cannot be reassigned new values because it is consistant. If we try to reassign it a value, it will throw a TypeError.
//const must also be assigned a value when declared, unline var and letter
