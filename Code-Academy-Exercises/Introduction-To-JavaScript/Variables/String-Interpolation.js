let myName = 'Kristi';
let myCity = 'Denver';

console.log(`My name is ${myName}. My favorite city is ${myCity}`);

// We can insert vaiables into strings using template literals.
// Template literals are wrapped with backticks
//the variable is wrapped in a ${}
// Template literals make code easier to read
