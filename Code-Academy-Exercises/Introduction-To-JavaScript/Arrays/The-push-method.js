const chores = ['wash dishes', 'do laundry', 'take out trash'];


chores.push('mop', 'clean bathroom')

console.log(chores);

// the push built in method allows us to add items to the end of the array. The item we want to add would be put into the ().
