var numberClusters = [[1,2], [3,4], [5,6]];

const target = numberClusters[2][1];
console.log(target);

// When there is an array within another array this is called a nested array.
// nested arrays can be called using bracket notation.
// if we want to call the elements in a nested array, we would chain more bracket notations with the index value.
