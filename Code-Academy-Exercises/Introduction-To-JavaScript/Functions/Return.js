function monitorCount(rows, columns){
  return rows * columns;
}

const numOfMonitors = monitorCount(5,4);

console.log(numOfMonitors)

//The return keyword produces an output that can be saved to a variable for later used
// a function will by default return undefined.
