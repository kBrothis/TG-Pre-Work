const plantNeedsWater = (day) => {
  if (day === 'Wednesday') {
    return true;
  } else {
    return false;
  }
};


// Arrow function syntax is a shorter way to write a function using the "fat arrow" notation: ()=>
// Arrow functions remove the need to write function every time we create a function.
// the parameters go into the () and then the arrow points to the body in {}
