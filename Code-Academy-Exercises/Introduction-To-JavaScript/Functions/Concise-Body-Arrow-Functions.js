const plantNeedsWater = day => day === 'Wednesday' ? true : false;

// Concise body is a condensed form of the function.
// if functions have zero or multiple parameters then the parenthese are needed but if there is only one parameter, the parentheses are not required.
// A single line body does not need curly braces in the function. The contents would follow the arrow and the return keyword is not required
