function makeShoppingList(item1 = 'milk', item2 ='bread', item3 = 'eggs'){
  console.log(`Remember to buy ${item1}`);
  console.log(`Remember to buy ${item2}`);
  console.log(`Remember to buy ${item3}`);
}

// Default parameters allow for a parameter to have a default value in case there isn't an arguement passed inspect.
//The default parameter would be set up to = a value in the ()
