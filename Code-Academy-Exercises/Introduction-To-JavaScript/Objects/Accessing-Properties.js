let spaceship = {
  homePlanet: 'Earth',
  color: 'silver',
  'Fuel Type': 'Turbo Fuel',
  numCrew: 5,
  flightPath: ['Venus', 'Mars', 'Saturn']
};

// Write your code below

let crewCount = spaceship.numCrew;
let planetArray = spaceship.flightPath;

// We can access a property by using dot notation. we would writ the object's name, a dot, followed by the property name.
