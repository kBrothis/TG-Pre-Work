let mood = 'sleepy';
let tirednessLevel = 6;


if(mood === 'sleepy' || tirednessLevel >8){
  console.log('time to sleep');
}else{
  console.log('not bed time yet')
}

// Logical operators work with boolean values to add more logic to the conditonals
//there are three logical operators: && - and operator checks if two conditions are met, || - or operator checks to see if at least one of the conditions is met, ! - not operators checks to see if the reverse of the condition is met;
